{
  "name": "@fraserelliott/fail",
  "version": "1.0.8",
  "description": "Fraser's Awesome Input Library - a validation library for any fields",
  "main": "fail-schema.js",
  "scripts": {
    "test": "jest",
    "build:browser": "npx esbuild fail.js --bundle --minify --outfile=dist/fail.esm.js --format=esm"
  },
  "keywords": [
    "validation",
    "schema",
    "form-validation",
    "field-validation",
    "validator",
    "rules",
    "javascript"
  ],
  "author": "Fraser Elliott",
  "license": "ISC",
  "devDependencies": {
    "jest": "^30.0.3"
  }
}